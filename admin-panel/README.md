# admin-panel

Internal admin UI for moderation and operations. Scaffold only — wire to `server` `/api/admin/*` routes as features land.

**Dev:** `npm install && npm run dev` (port **3001**; public `website` uses **3000**.)
